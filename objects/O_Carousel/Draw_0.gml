// Center of the carousel
var center_x = room_width / 2;
var center_y = room_height / 2;

// Radius of the carousel
var radius = 300;

// Rotation speed (in degrees per step)
var rotation_speed = 2;

// Draw the carousel
draw_carousel(carousel_items, center_x, center_y, radius, rotation_speed);